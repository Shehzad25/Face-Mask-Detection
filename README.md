# Face-Mask-Detection
Built a face mask classification system using MobileNetV2 transfer learning. Preprocessed and augmented images, trained a CNN-based binary classifier (mask/no mask), evaluated using accuracy and confusion matrix, and saved the model for deployment using TensorFlow and Keras.
